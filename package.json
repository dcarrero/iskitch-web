{
  "name": "iskitch-web",
  "version": "1.0.0",
  "description": "iskitch.com — landing page and privacy/support pages for iSkitch (macOS app).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "og": "node scripts/generate-og.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "sync": "node --env-file=.env scripts/sync-acumbamail.mjs"
  },
  "devDependencies": {
    "astro": "^6.4.8",
    "sharp": "^0.34.5"
  }
}
